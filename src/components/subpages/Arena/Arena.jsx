import { NavLink } from "react-router-dom";
import { Button } from "../../shared/Button";

export function Arena({ className }) {
  return (
    <>
      <Button className="rounded bg-sky-500 px-3 py-1 text-white">Arena</Button>
    </>
  );
}
